# Admin Teams

This page is retained for legacy links. The admin dashboard now uses [Admin Workspaces](admin-workspaces.md).

Legacy `/admin/teams`, `/admin/teams/create`, and `/admin/teams/invites` routes redirect to `/admin/workspaces`, `/admin/workspaces/create`, and `/admin/workspaces/invites`.
